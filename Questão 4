programa {
  funcao inicio() {
    real salario

    escreva("Digite seu salário:")
    leia(salario)

    salario = salario+(salario * 0.10)

    escreva("Seu salário com reajuste de 10%:",salario)
  }
}
